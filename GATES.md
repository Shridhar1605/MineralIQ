# GATES.md — gate log (evidence for "results vs KPIs")
| Date | Stage | Result | Metrics / notes |
|---|---|---|---|
| 2026-09-17 | s0 | PASS (docs) | Idea docs fixed: SPOC e-mail → vit.ac.in; §3 faculty+students wording; InPASS → manual-only; single-team rule noted |
| 2026-09-17 | s1 | PASS (local) | `make regress`: 11 passed. taxonomy v1 valid (5 minerals/6 stages/22 families); fixtures 8+4 seed load w/ provenance; schema contract v1; compose 1-cmd; /health shape; CI workflow added. Remaining pre-2-Oct: BigQuery dry run, 5 R&D fetches, volume + memory checks, expand golden to ~200 |
| 2026-09-17 | s2 | PASS (local) | `make regress`: 15 passed, s1 still green. 7 adapters parse saved samples (no live calls); completeness 100% (gate >=95%); 14 parsed / 14 deduped / 0 duplicates; re-run adds zero; output respects UNIQUE(source_id, source_url). Live 2,000-record harvest deferred to Week 1 per plan |
| 2026-09-17 | s3 | PASS provisional (local) | `make regress`: 19 passed, s1+s2 still green. Lexicon baseline on frozen n=4 hold-out: mineral acc 0.75 / F1 0.80, stage acc 1.00 / F1 1.00 — reported, NOT gated (thresholds enforce at Week-2 n>=100). Org merge 100% on 14-org seed (gate >=90%), top20 collision-free, CSIR HQ != CSIR-NML. Graph: 12/12 record nodes, 0 dangling, co-file edge verified. `docs/labeling_guide.md` sets the Week-2 ≥80% agreement protocol |
| 2026-09-17 | s4 | PASS (local) | `make regress`: 24 passed, s1-s3 still green. 20/20 golden queries top-1 (frozen append-only contract); contract shapes fixed for search/record/summary/heatmap/org; avg search <1ms (gate <1s, re-measure at scale Week 3); dashboard totals == direct counts; classifier metrics re-verified post-index. React app (4 tabs) builds clean; Playwright search→detail spec saved, live run Week 3 |
| 2026-09-17 | s5 | PASS (local) | `make regress`: 28 passed, s1-s4 still green. 5/5 sampled matrix cells match hand calc (`fixtures/gap_handcheck.json`); whitespace rank sorted by research-patents gap, neglected flagged; collaborations share cells with no prior co-filing; synthetic LI patent fires exactly 1 alert, repeat fires 0 (seen-state). Gaps tab builds clean. OPEN: mentor/faculty sanity sign-off on top-3 white spaces (Week 4) |
| 2026-09-17 | s6 | PASS (local) — FEATURE FREEZE | `make regress`: 31 passed, all stages green. Nightly (harvest+evaluate+graph+backup) exits 0 unattended; backup 21 files, verify ok, restore byte-identical, tamper detected. Section-8 checklist: report, architecture.mmd, generated deck, demo script all present. README deploy-from-scratch + .env.example done. OPEN human-track: cloud deploy + regress against it, demo recording, mentor sign-off, upload by midday 30 Oct |
| 2026-09-21 | s1-s6 | PASS (52 tests) + gates now enforce | Steps 6-11 of the recovery plan. Container image BUILDS AND RUNS for the first time: `docker compose up -d --build` brings up db+api, schema_v1 applies on first boot (5 tables, source_url/fetched_at NOT NULL), /health and /search answer through the stack. Web app reaches the API end to end (docs/screenshots/app_running.png). Harvested records now reach search/graph/gaps (23 raw -> 21 after cross-source dedupe on normalised application number; the same IN application from PATENTSCOPE and Google Patents was being double-counted). Alert seen-state is per subscription (late subscribers were permanently deaf). fetched_at resolves to today, provenance is in the completeness gate. Macro-F1 no longer inflated by unexercised labels: stage F1 1.00 was resting on 4 untested labels, mineral F1 0.80 -> 0.75 honest. s3 threshold enforces at n>=100 (was PROVISIONAL forever, a 0%% classifier passed). Metric drift rule implemented and mutation-checked: 2 pp quality / 20%% speed, `make regress` runs it. |
| 2026-09-23 | s1 spike / s2 | Patent source REPLACED | Google Patents BigQuery FAILED the Stage 1 volume check (83,504 IN records, 238 with a title, 0 pilot matches; 35 GB billed). EPO OPS and Lens.org set aside (payment / slow approval). PRIMARY source is now the Official Journal of the Patent Office: public, no login or CAPTCHA, robots.txt permissive; downloads replay only page-provided FileName values (forged values refused in code). Issue 38/2026: 2,753 applications in Parts I+II, parser 100% on appl_no/title/abstract/applicants/IPC/dates, inventors 99.3%. 148 candidates by the recall rule; lexicon labels 83 but by-eye precision is about 1 in 5 (e.g. 'Co-Doped' matched cobalt, a lunchbox matched lithium). The n=5 synthetic hold-out could not reveal this; Week 2 must label REAL Journal records. Backups now exclude the regenerable PDF cache and verify scratch (1.4 MB, was set to grow by ~43 MB/week). 65 tests. |
